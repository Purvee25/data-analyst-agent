# Pinned versions for reproducible installs (local, CI, and Streamlit Cloud).
# Pinning is a production requirement: an unpinned dep can silently change
# behaviour between a passing CI run and a broken deploy.
pandas==2.2.3
matplotlib==3.10.0
anthropic==0.116.0
mcp==1.28.1
pytest==8.3.4

# React-frontend backend (api/): serves the analyst pipeline as JSON.
fastapi==0.139.0
uvicorn==0.51.0
python-multipart==0.0.32

# streamlit==1.41.1  # legacy Streamlit UI (app.py) — kept but superseded by the React frontend.
streamlit==1.41.1
